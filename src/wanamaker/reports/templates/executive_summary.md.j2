{# Plain-English executive summary template (FR-5.3).

   This is a placeholder skeleton. The real template lands in Phase 2 once
   the posterior summary structures are concrete. The voice, per AGENTS.md
   §"a note on the project's voice": dry, direct, honest about uncertainty,
   no AI sparkle phrases.

   Required context keys (to be finalized):
     - period_start, period_end
     - total_spend, total_target
     - channels: [{name, contribution, ci, confidence}]
     - trust_card: TrustCard
#}
# Executive summary

_Period: {{ period_start }} – {{ period_end }}_

This is a placeholder. The real summary template lands in Phase 2.
