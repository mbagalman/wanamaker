"""Data input and schema validation.

Owns the CSV interface (FR-1.1), channel taxonomy (FR-1.2), lift-test
calibration input (FR-1.3), and control-variable handling (FR-1.4).

Per AGENTS.md Hard Rule 6, the CSV interface is the user contract — do
not redesign input/output formats without revisiting the PRD.
"""
