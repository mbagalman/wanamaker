"""Bundled example datasets accessed via ``importlib.resources``.

The leading underscore signals that this is internal package data,
not a user-facing API surface. ``wanamaker run --example <name>``
materialises one of the subpackages here (e.g. ``public_benchmark``)
into a working artifact directory; see ``cli.run`` for the wiring.
"""
