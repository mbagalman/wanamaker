"""Bundled public-benchmark example: CSV, YAML config, and metadata JSON.

Files in this package are loaded as resources via ``importlib.resources``
when the user runs ``wanamaker run --example public_benchmark``.
"""
